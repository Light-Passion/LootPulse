<Window x:Class="LootPulse.HudWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="LootPulse HUD"
        Height="120" Width="250"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">
    
    <Grid>
        <!-- HUD Outer Border with Rounded Corners -->
        <Border x:Name="HudOuterBorder" CornerRadius="12" BorderBrush="#3D3D3D" BorderThickness="1.5" Background="#F2121212">
            <!-- Viewbox handles scaling all content proportionally on window resize -->
            <Viewbox x:Name="HudViewbox" Stretch="Uniform" Margin="10">
                <Grid Width="220" Height="90">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="20"/> <!-- Mini Title -->
                        <RowDefinition Height="*"/>  <!-- Main Details -->
                        <RowDefinition Height="18"/> <!-- Status Footer -->
                    </Grid.RowDefinitions>

                    <!-- Row 0: Mini Title & Indicator -->
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <Ellipse Width="6" Height="6" Fill="#FF6124" Margin="0,0,6,0"/>
                        <TextBlock Text="LOOTPULSE HUD" Foreground="#888888" FontSize="9" FontWeight="Bold"/>
                    </StackPanel>
                    
                    <TextBlock x:Name="DragNoticeText" Text="[Drag to Position]" Foreground="#555555" FontSize="9" HorizontalAlignment="Right" VerticalAlignment="Center" Visibility="Visible"/>

                    <!-- Row 1: Active Zone & Level -->
                    <StackPanel Grid.Row="1" VerticalAlignment="Center" Margin="0,5">
                        <TextBlock x:Name="HudZoneText" Text="Lioneye's Watch" Foreground="White" FontSize="16" FontWeight="Bold" TextTrimming="CharacterEllipsis"/>
                        <StackPanel Orientation="Horizontal" Margin="0,2,0,0">
                            <TextBlock Text="Level " Foreground="#E5B560" FontSize="12" FontWeight="SemiBold"/>
                            <TextBlock x:Name="HudLevelText" Text="1" Foreground="#E5B560" FontSize="12" FontWeight="Bold"/>
                            <TextBlock x:Name="HudCharNameText" Text=" (Exile)" Foreground="#AAAAAA" FontSize="12" Margin="5,0,0,0" TextTrimming="CharacterEllipsis"/>
                        </StackPanel>
                    </StackPanel>

                    <!-- Row 2: Status Footer -->
                    <TextBlock Grid.Row="2" x:Name="HudStatusText" Text="Overlay Active" Foreground="#666666" FontSize="9.5" VerticalAlignment="Bottom" TextTrimming="CharacterEllipsis"/>
                </Grid>
            </Viewbox>
        </Border>
    </Grid>
</Window>
